# OKF-AT 準拠チェックのベースライン allowlist
#
# OKF-AT（docs/okf-at/README.md）導入時点で非準拠だった既存ファイルの一覧。
# ここに載っているファイルの違反は警告のみで CI を fail させない（段階移行のため）。
# frontmatter を付与して準拠させたら、この一覧から削除して allowlist を縮小していく。
# 新規ファイルを追加してはならない（新規ファイルは最初から準拠させる）。
#
# 経緯: adr/20260713-adopt-okf-at-knowledge-format/README.md
#
# 導入時に既存ファイルを一括移行したため、現在は空。空の状態を維持すること。
